import 'package:flutter/material.dart';

import '../constant/header.dart';

class VideoWallpaperScreen extends StatelessWidget {
  const VideoWallpaperScreen({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Header(
      title: "Video Wallpapers",
    );
  }
}